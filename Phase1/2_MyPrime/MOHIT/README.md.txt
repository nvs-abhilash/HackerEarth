**MY PRIMES**

	*It is a basic program of time complexity of O(n^2).
	*It contains a set of numbers as input.
	*It has its own definition of prime numbers.
	*For a given set of positive integers S let's call X a prime if there are no elements in S which are divisors of X (except X itself).